/* Math é um objeto
    que possui uma constante chamada PI
    e uma função que calcula potência (pow)
*/
const raio = 5.6;
const area = Math.PI * Math.pow(raio,2);
console.log(area);
console.log(typeof Math);